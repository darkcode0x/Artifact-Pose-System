kiểm tra cho tôi về kĩ thuật trong việc góc servo điều chỉnh
là độ xoay góc và log được in trong trong embedded system sau khi chạy mainapp.py để kiểm tra kỹ thuật trong việc góc servo điều chỉnh
là độ lệch tính ra sẽ điều chỉnh pantilt như thế nào, tại sao log lại ghi ra chữ target. 
tóm lại là khi khởi tạo, có tự động điều chỉnh servo về góc ban đầu trước khi chụp ảnh chuẩn hay không, và tại sao log lại ghi ra chữ target.

và liệu tính toán có chuẩn chỉ hay không, có cần phải điều chỉnh gì thêm không để đảm bảo rằng góc servo được điều chỉnh chính xác theo độ lệch tính ra từ việc chụp ảnh chuẩn.

đọc lại kĩ và phân tích kỹ thuật về việc điều chỉnh góc servo trong hệ thống nhúng sau khi chạy mainapp.py.
